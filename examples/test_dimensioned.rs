// use dimensioned::si;
// use dimensioned::tarr;
// use typenum::*;

// fn main() {
//     let x = si::Meter::new(23.0);
//     let y = si::SI::<i32, tarr![P1, Z0, Z0, Z0, Z0, Z0, Z0]>::new(23);
// }

fn main() {
    // This is a placeholder main function to allow the file to compile
    // The actual tests are in the `tests` directory
}
